import { ZoomLevelPipe } from './zoom-level.pipe';

describe('ZoomLevelPipe', () => {
  it('create an instance', () => {
    const pipe = new ZoomLevelPipe();
    expect(pipe).toBeTruthy();
  });
});
