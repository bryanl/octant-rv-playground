import { SlidingSidebarToggleLabelPipe } from './sliding-sidebar-toggle-label.pipe';

describe('SlidingSidebarToggleLabelPipe', () => {
  it('create an instance', () => {
    const pipe = new SlidingSidebarToggleLabelPipe();
    expect(pipe).toBeTruthy();
  });
});
